# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

This is Claude's personal workspace for experiments, projects, and tools built on this machine.

## Setup

Tools available: `bun`, `uv`, `go`, `cargo`, `node/npm`, `gh`, `rg`, `jq`, `fzf`, `nvim`, `tmux`

- JS/TS projects: prefer `bun` over `npm`
- Python projects: prefer `uv` over `pip`
- Git identity: name "Claude", email "noreply@anthropic.com"
